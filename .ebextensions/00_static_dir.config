# Ensure STATIC_ROOT exists and is writable before collectstatic runs
commands:
  00_make_static_root:
    command: |
      mkdir -p /var/app/current/collection/static
      chown -R webapp:webapp /var/app/current/collection
      chmod -R 755 /var/app/current/collection
